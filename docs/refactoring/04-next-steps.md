# План рефакторинга: Следующие шаги

Текущий статус: Ядро (`js/core`) и Состояние (`js/state`) полностью переведены на ES Modules.
`js/app.js` является точкой входа и обеспечивает обратную совместимость через "мост" (`window.*`).

## Фаза 3: Переход на ES Modules (В процессе) 🚧

### 1. Ядро (Выполнено) ✅
- [x] `js/core/*.js` сконвертированы в модули.
- [x] `index.html` обновлен (удалены теги скриптов ядра).

### 2. Состояние (Выполнено) ✅
- [x] `js/state/*.js` сконвертированы в модули (кроме `appState.js`, `battleState.js`, которые оставлены как глобальные контейнеры).
- [x] `js/app.js` импортирует модули состояния и экспортирует их в `window` для совместимости.
- [x] `index.html` обновлен (удалены теги скриптов состояния).

### 3. UI (Следующий шаг)
- [ ] Конвертировать `js/ui/*.js` в ES Modules:
  - `components.js`
  - `modals.js`
  - `toasts.js`
  - `tooltips.js`
  - `animations.js` (effects)
  - `ui.js` (общий UI код)
- [ ] Настроить импорты в `app.js` и экспорт в `window.UI`.

### 4. Экраны (Следующий шаг)
- [ ] Конвертировать экраны `js/screens/*.js` в ES Modules.
- [ ] Конвертировать `js/monsters.js` и `js/config.js` (старые файлы данных).

### 5. Очистка
- [ ] Удалить "мост" из `js/app.js`, когда все файлы будут модульными и смогут импортировать зависимости напрямую.
- [ ] Удалить `js/state/appState.js` и перенести роутер в модуль.

## Фаза 4: UI/UX Полировка

- [ ] Внедрить CSS переменные для цветов и размеров.
- [ ] Улучшить анимации (CSS transitions вместо JS анимаций где возможно).
